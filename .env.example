# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/genesis-flyer

# Base URL Puppeteer uses to hit the print route when generating a PDF.
# In production, set this to your deployed URL (e.g. https://flyers.genesisgroup.ng)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
